import type { CSSProperties } from "react";import { Skeleton } from "@/components/ui/skeleton";/** Grid of 4 card skeletons — used for Why Us / Services / Testimonials */export function CardGridSkeleton({ count = 4 }: { count?: number }) {  return (    <div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-6">      {Array.from({ length: count }).map((_, i) => (        <div key={i} className="rounded-2xl border border-border bg-card p-7 space-y-4">          <Skeleton className="h-12 w-12 rounded-xl" />          <Skeleton className="h-5 w-3/4" />          <Skeleton className="h-4 w-full" />          <Skeleton className="h-4 w-5/6" />        </div>      ))}    </div>  );}/** Full-width section header skeleton */export function SectionHeaderSkeleton() {  return (    <div className="text-center max-w-3xl mx-auto space-y-3">      <Skeleton className="h-3 w-20 mx-auto" />      <Skeleton className="h-8 w-2/3 mx-auto" />    </div>  );}/** FAQ list skeleton */export function FaqSkeleton({ count = 4 }: { count?: number }) {  return (    <div className="space-y-3">      {Array.from({ length: count }).map((_, i) => (        <div key={i} className="rounded-xl border border-border bg-card p-5 space-y-3">          <Skeleton className="h-4 w-3/4" />          <Skeleton className="h-3 w-full" />          <Skeleton className="h-3 w-5/6" />        </div>      ))}    </div>  );}/** Process row skeleton (4 steps) */export function ProcessSkeleton() {  return (    <div className="grid md:grid-cols-4 gap-6">      {Array.from({ length: 4 }).map((_, i) => (        <div key={i} className="relative rounded-2xl border border-border bg-card p-7 space-y-4">          <Skeleton className="absolute top-4 right-5 h-12 w-12" />          <Skeleton className="h-5 w-3/4" />          <Skeleton className="h-4 w-full" />          <Skeleton className="h-4 w-5/6" />        </div>      ))}    </div>  );}/** Gallery grid skeleton */export function GallerySkeleton({ count = 8 }: { count?: number }) {  return (    <div className="grid grid-cols-2 md:grid-cols-4 gap-3 md:gap-4">      {Array.from({ length: count }).map((_, i) => (        <div key={i} className="aspect-square rounded-2xl">          <Skeleton className="h-full w-full" />        </div>      ))}    </div>  );}/** Service card grid skeleton */export function ServiceSkeleton({ count = 6 }: { count?: number }) {  return (    <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">      {Array.from({ length: count }).map((_, i) => (        <div key={i} className="rounded-2xl border border-border bg-card p-7 space-y-4">          <Skeleton className="h-14 w-14 rounded-2xl" />          <Skeleton className="h-5 w-3/4" />          <Skeleton className="h-4 w-full" />          <Skeleton className="h-4 w-5/6" />        </div>      ))}    </div>  );}