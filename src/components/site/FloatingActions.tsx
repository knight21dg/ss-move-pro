import { MessageCircle, Phone } from "lucide-react";import { useSettings } from "@/hooks/use-cms";export function FloatingActions() {  const { data: s } = useSettings();  const { phone, whatsapp } = s?.contact ?? {};  const cta = s?.cta;  const digits = (v: string) => v.replace(/\D/g, "");  const waDigits = whatsapp ? digits(whatsapp) : "";  const phoneDigits = phone ? digits(phone) : "";  if (!phone && !whatsapp) return null;  return (    <div className="fixed bottom-5 right-5 z-50 flex flex-col gap-3">      {whatsapp && (        <a          href={`https://wa.me/${waDigits}?text=Hi%20SS%20Packers%20%26%20Movers%2C%20I%27d%20like%20a%20quote`}          target="_blank" rel="noreferrer"          aria-label="WhatsApp"          className="h-14 w-14 rounded-full text-white flex items-center justify-center shadow-brand hover:scale-110 transition-transform"          style={{ backgroundColor: cta?.whatsapp_bg_color || "oklch(0.7 0.18 145)" }}        >          <MessageCircle className="h-6 w-6" />        </a>      )}      {phone && (        <a          href={`tel:${phoneDigits}`}          aria-label="Call"          className="h-14 w-14 rounded-full text-primary-foreground flex items-center justify-center shadow-brand hover:scale-110 transition-transform"          style={{ backgroundColor: cta?.call_bg_color || "var(--primary)" }}        >          <Phone className="h-6 w-6" />        </a>      )}    </div>  );}