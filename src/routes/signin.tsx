import { createFileRoute, useNavigate, Link } from "@tanstack/react-router";import { useEffect, useState } from "react";import { onAuthChange, signIn, signUp, signOutUser, resetPassword, updateUserPassword } from "@/lib/firebase";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { toast } from "sonner";import logo from "@/assets/logo.png";import { Eye, EyeOff } from "lucide-react";export const Route = createFileRoute("/signin")({  head: () => ({ meta: [{ title: "Admin Login — SS Packers & Movers" }] }),  component: LoginPage,});type AuthAction = "signin" | "signup" | "reset" | "update";const AUTH_REDIRECT_URL =  import.meta.env.VITE_AUTH_REDIRECT_URL || "https://ss-move-pro-eight.vercel.app/signin";function getAuthErrorMessage(error: { message?: string } | null | undefined, action: AuthAction) {  const message = error?.message ?? "";  const n = message.toLowerCase();  if (n.includes("invalid login credentials")) return "That email and password do not match. Try again or reset your password.";  if (n.includes("email not confirmed")) return "Please confirm your email before signing in.";  if (n.includes("user already registered") || n.includes("already registered")) return "An account with this email already exists. Sign in instead.";  if (n.includes("password should be at least") || n.includes("password too short")) return "Password must be at least 6 characters.";  if (n.includes("invalid email")) return "Enter a valid email address.";  if (n.includes("signup is disabled")) return "Account creation is disabled. Contact an administrator.";  if (n.includes("rate limit") || n.includes("too many requests")) return "Too many attempts. Please wait a few minutes and try again.";  if (n.includes("auth session missing") || n.includes("session not found")) return "Your reset link has expired. Request a new one.";  if (n.includes("token") && n.includes("expired")) return "Your reset link has expired. Request a new one.";  if (n.includes("network")) return "Network error. Check your connection and try again.";  switch (action) {    case "reset": return "We could not send a reset link. Please try again.";    case "update": return "We could not update your password. Please try again.";    case "signup": return "We could not create the account. Please try again.";    case "signin": return "We could not sign you in. Please try again.";  }  return message || "Something went wrong. Please try again.";}function LoginPage() {  const [mode, setMode] = useState<"signin" | "signup">("signin");  const [email, setEmail] = useState("");  const [password, setPassword] = useState("");  const [showPassword, setShowPassword] = useState(false);  const [loading, setLoading] = useState(false);  const [resetting, setResetting] = useState(false);  const [recoveryMode, setRecoveryMode] = useState(false);  const [newPassword, setNewPassword] = useState("");  const [confirmPassword, setConfirmPassword] = useState("");  const navigate = useNavigate();  useEffect(() => {    const unsub = onAuthChange((user) => {      if (user && !recoveryMode) navigate({ to: "/admin" });    });    return () => { unsub(); };  }, [navigate, recoveryMode]);  async function onSubmit(e: React.FormEvent) {    e.preventDefault();    if (!email.trim()) { toast.error("Enter your email to continue."); return; }    if (!password.trim()) { toast.error("Enter your password to continue."); return; }    if (password.length < 6) { toast.error("Password must be at least 6 characters."); return; }    setLoading(true);    try {      await (mode === "signin" ? signIn(email, password) : signUp(email, password));      toast.success(mode === "signin" ? "Welcome back." : "Account created.");      navigate({ to: "/admin" });    } catch (err: any) {      toast.error(getAuthErrorMessage(err, mode));    } finally {      setLoading(false);    }  }  async function sendResetLink() {    if (!email.trim()) { toast.error("Enter your email to receive a reset link."); return; }    setResetting(true);    try {      await resetPassword(email);      toast.success("Reset link sent. Check your email.");    } catch (err: any) {      toast.error(err.message || "Failed to send reset link.");    } finally {      setResetting(false);    }  }  async function onUpdatePassword(e: React.FormEvent) {    e.preventDefault();    if (!newPassword.trim()) { toast.error("Enter a new password."); return; }    if (newPassword.length < 6) { toast.error("Password must be at least 6 characters."); return; }    if (newPassword !== confirmPassword) { toast.error("Passwords do not match."); return; }    setLoading(true);    try {      await updateUserPassword(newPassword);      toast.success("Password updated. Please sign in.");      await signOutUser();      setRecoveryMode(false);      setNewPassword("");      setConfirmPassword("");      setPassword("");      navigate({ to: "/signin" });    } catch (err: any) {      toast.error(err.message || "Password update failed.");    } finally {      setLoading(false);    }  }  return (    <div className="min-h-screen flex items-center justify-center bg-gradient-dark px-4">      <div className="w-full max-w-md rounded-2xl bg-card p-8 shadow-xl border border-border">        <Link to="/" className="flex justify-center mb-6"><img src={logo} alt="SS Packers" className="h-14 w-auto" /></Link>        <h1 className="text-2xl font-bold text-center mb-1">{recoveryMode ? "Reset Password" : mode === "signin" ? "Admin Sign In" : "Create Admin Account"}</h1>        <p className="text-sm text-muted-foreground text-center mb-6">{recoveryMode ? "Set a new password." : "Manage your site content"}</p>        <form onSubmit={recoveryMode ? onUpdatePassword : onSubmit} className="space-y-4">          {recoveryMode ? (            <>              <div className="relative">                <Label htmlFor="new-password">New Password</Label>                <Input id="new-password" type={showPassword ? "text" : "password"} required minLength={6} value={newPassword} onChange={(e) => setNewPassword(e.target.value)} className="mt-1.5" />                <button type="button" onClick={() => setShowPassword(!showPassword)} className="absolute inset-y-0 right-0 top-6 flex items-center px-4 text-muted-foreground">{showPassword ? <EyeOff className="h-5 w-5" /> : <Eye className="h-5 w-5" />}</button>              </div>              <div>                <Label htmlFor="confirm-password">Confirm Password</Label>                <Input id="confirm-password" type={showPassword ? "text" : "password"} required minLength={6} value={confirmPassword} onChange={(e) => setConfirmPassword(e.target.value)} className="mt-1.5" />              </div>              <Button type="submit" variant="brand" className="w-full" disabled={loading}>{loading ? "Updating..." : "Update Password"}</Button>            </>          ) : (            <>              <div><Label htmlFor="email">Email</Label><Input id="email" type="email" required value={email} onChange={(e) => setEmail(e.target.value)} className="mt-1.5" /></div>              <div className="relative">                <Label htmlFor="password">Password</Label>                <Input id="password" type={showPassword ? "text" : "password"} required minLength={6} value={password} onChange={(e) => setPassword(e.target.value)} className="mt-1.5" />                <button type="button" onClick={() => setShowPassword(!showPassword)} className="absolute inset-y-0 right-0 top-6 flex items-center px-4 text-muted-foreground">{showPassword ? <EyeOff className="h-5 w-5" /> : <Eye className="h-5 w-5" />}</button>              </div>              <Button type="button" variant="link" size="sm" className="px-0" onClick={sendResetLink} disabled={resetting}>{resetting ? "Sending reset link..." : "Forgot password? Send reset link"}</Button>              <Button type="submit" variant="brand" className="w-full" disabled={loading}>{loading ? "Please wait..." : mode === "signin" ? "Sign In" : "Sign Up"}</Button>            </>          )}        </form>        {!recoveryMode && (          <button type="button" onClick={() => setMode(mode === "signin" ? "signup" : "signin")} className="mt-4 text-sm text-primary hover:underline w-full text-center">            {mode === "signin" ? "Need an account? Sign up" : "Already have an account? Sign in"}          </button>        )}        <div className="mt-6 text-xs text-muted-foreground text-center">          <Link to="/" className="hover:text-foreground">← Back to website</Link>        </div>      </div>    </div>  );}