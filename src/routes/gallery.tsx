import { createFileRoute } from "@tanstack/react-router";import { SiteLayout } from "@/components/site/Layout";import { PageHero } from "./about";import {  useGallery,  useSettings,  galleryQueryOptions,  settingsQueryOptions,} from "@/hooks/use-cms";import { optimizeCloudinaryUrl } from "@/lib/cloudinary";export const Route = createFileRoute("/gallery")({  head: () => ({    meta: [      { title: "Gallery — SS Packers & Movers" },      {        name: "description",        content: "Photos from our packing, moving, warehousing and vehicle transport operations.",      },    ],  }),  loader: async ({ context }) => {    try {      await Promise.all([        context.queryClient.ensureQueryData(settingsQueryOptions()),        context.queryClient.ensureQueryData(galleryQueryOptions(true)),      ]);    } catch (error) {      console.error("Error prefetching data for gallery route:", error);    }  },  component: GalleryPage,});function GalleryPage() {  const { data: items = [] } = useGallery();  const { data: s } = useSettings();  const heroImage = s?.hero_images?.gallery;  return (    <SiteLayout>      <PageHero        eyebrow="Gallery"        title="A look at our work"        subtitle="Real moments from real moves we've handled across India."        backgroundImage={heroImage}      />      <section className="container mx-auto px-4 py-16 md:py-20">        {items.length === 0 ? (          <p className="text-center text-muted-foreground">No gallery images yet.</p>        ) : (          <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4 md:gap-6">            {items.map((it) => (              <div key={it.id} className="relative group overflow-hidden rounded-2xl aspect-[4/3]">                <a                  className="h-full w-full block"                  onClick={(e) => {                    e.preventDefault();                    import("@/components/site/Lightbox").then((m) =>                      m.openLightbox({                        type: "image",                        src: optimizeCloudinaryUrl(it.image_url, 1200),                        title: it.title,                      }),                    );                  }}                >                  <img                    src={optimizeCloudinaryUrl(it.image_url, 600)}                    alt={it.title ?? ""}                    loading="lazy"                    className="h-full w-full object-cover group-hover:scale-110 transition-transform duration-500"                  />                </a>                {(it.title || it.category) && (                  <>                    <div className="absolute inset-0 bg-gradient-to-t from-black/70 via-transparent to-transparent" />                    <div className="absolute bottom-4 left-4 text-white">                      {it.title && <div className="font-semibold">{it.title}</div>}                      {it.category && <div className="text-xs text-white/80">{it.category}</div>}                    </div>                  </>                )}              </div>            ))}          </div>        )}      </section>    </SiteLayout>  );}